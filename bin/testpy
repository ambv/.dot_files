#!/bin/bash -ex
# cd $1
# shift
date
export PYTHONBINARY=/tmp/cpython-root-$(git branch --show-current)/bin/python3
$PYTHONBINARY -V
PYTHONWARNINGS= PYTHONSTARTUP= IN_REGRTEST=1 time $PYTHONBINARY -E -Wd -m test --slowest --fail-env-changed $*
